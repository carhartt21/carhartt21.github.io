# GitHub Pages does not need Jekyll when using plain HTML.
# Disable Jekyll processing so index.html is served directly.
exclude:
  - "*.tex"
  - "*.aux"
  - "*.log"
  - "*.out"
  - "*.doc"
  - data_and_certificates/
  - links.txt
  - publications.tex
